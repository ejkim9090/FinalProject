package kh.spring.s02.model.controller;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpSession;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.servlet.ModelAndView;

import kh.spring.s02.model.service.BoardService;
import kh.spring.s02.model.vo.BoardVo;

@Controller
@RequestMapping("/board", method=RequestMethod.GET)
public class BoardController {
	@Autowired
	private BoardService service;
	
	@RequestMapping(value="/update", method=RequestMethod.GET)
	public void viewUpdateBoard(Model model) {
		
	}
	
	@RequestMapping(value="/delete", method=RequestMethod.GET)
	public void viewDeleteBoard() {
		
	}
	@RequestMapping(value="/list", method=RequestMethod.GET)
	public void viewlist(Model model) {
		model.addAttribute("boardlist", service.selectList());
	}
	
	@GetMapping("/boardinsert")
	public void viewinsertBoard(
			ModelAndView mv
			, HttpServletRequest req
			, HttpSession session
			, BoardVo vo
			) {
		
		
		//mv.addObject("test", "text value");
		return;
	}
	
	@PostMapping("/boardinsert")
	public ModelAndView doinsertBoard(ModelAndView mv) {
		
		return mv;
	}
}
